::deepgram::integration::ctx ns

::env ::hot::env

// Placeholder credentials for the deepgram integration suite. Empty strings
// are falsy, so unset variables behave exactly like absent context.
::hot::ctx/set({
    "deepgram.api.key": ::env/get("DEEPGRAM_API_KEY", ""),
})

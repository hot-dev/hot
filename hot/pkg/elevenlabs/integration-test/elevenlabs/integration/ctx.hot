::elevenlabs::integration::ctx ns

::env ::hot::env

// Placeholder credentials for the elevenlabs integration suite. Empty strings
// are falsy, so unset variables behave exactly like absent context.
::hot::ctx/set({
    "elevenlabs.api.key": ::env/get("ELEVENLABS_API_KEY", ""),
})

::attio::integration::ctx ns

::env ::hot::env

// Placeholder credentials for the attio integration suite. Empty strings
// are falsy, so unset variables behave exactly like absent context.
::hot::ctx/set({
    "attio.api.key": ::env/get("ATTIO_API_KEY", ""),
})

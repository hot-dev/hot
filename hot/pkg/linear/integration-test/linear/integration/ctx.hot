::linear::integration::ctx ns

::env ::hot::env

// Placeholder credentials for the linear integration suite. Empty strings
// are falsy, so unset variables behave exactly like absent context.
::hot::ctx/set({
    "linear.api.key": ::env/get("LINEAR_API_KEY", ""),
})

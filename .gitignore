/target
/.data
node_modules/
.hot/
.skills/
hot/ctx.hot
hot/dev.key
hot/src-offline/
**/dev.key
*.pem
*.key
*.p12
*.pfx
*.tfvars
*.kubeconfig

## samply profiling
profile.json
profile.json.gz

# Per-test scratch dir. Tests that exercise the real `::hot::file`
# direct-mode backend (e.g. `::ai::agent::blob`) write here through an
# explicit prefix so output never lands in production-shaped paths.
**/.test-tmp/

# macOS specific files
.DS_Store

# Generated at build time
resources/git-revision.txt
resources/pkg-docs/
resources/bin/
crates/hot_cli/resources/
cdn-output/
.env
.env.*
!.env.example
.cursor/
::airtable::integration::ctx ns

::env ::hot::env

// Placeholder credentials for the airtable integration suite. Empty strings
// are falsy, so unset variables behave exactly like absent context.
::hot::ctx/set({
    "airtable.token": ::env/get("AIRTABLE_TOKEN", ""),
})

::firecrawl::integration::ctx ns

::env ::hot::env

// Placeholder credentials for the firecrawl integration suite. Empty strings
// are falsy, so unset variables behave exactly like absent context.
::hot::ctx/set({
    "firecrawl.api.key": ::env/get("FIRECRAWL_API_KEY", ""),
})

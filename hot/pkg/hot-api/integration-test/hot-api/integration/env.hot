::hot-api::integration ns
meta ["test"]

// Requires ctx: hot-api.key (+ hot-api.url for local)
test-environment-info
meta ["test"] fn () {
    out ::hot-api/environment-info()
    assert(not(is-err(out)), "key should resolve the environment")
}

::notion::integration::ctx ns

::env ::hot::env

// Placeholder credentials for the notion integration suite. Empty strings
// are falsy, so unset variables behave exactly like absent context.
::hot::ctx/set({
    "notion.token": ::env/get("NOTION_TOKEN", ""),
})

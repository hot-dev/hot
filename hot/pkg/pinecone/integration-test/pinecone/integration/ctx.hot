::pinecone::integration::ctx ns

::env ::hot::env

// Placeholder credentials for the pinecone integration suite. Empty strings
// are falsy, so unset variables behave exactly like absent context.
::hot::ctx/set({
    "pinecone.api.key": ::env/get("PINECONE_API_KEY", ""),
})

::notion::integration::ctx ns

::env ::hot::env

// Placeholder credentials for the notion integration suite. Unset
// environment variables are skipped entirely, so absent credentials
// behave exactly as if never configured.
::hot::ctx/set(filter-vals({
    "notion.token": ::env/get("NOTION_TOKEN", ""),
}, (v) { not(is-empty(v)) }))

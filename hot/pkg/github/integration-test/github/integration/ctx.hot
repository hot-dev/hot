::github::integration::ctx ns

::env ::hot::env

// Placeholder credentials for the github integration suite. Empty strings
// are falsy, so unset variables behave exactly like absent context.
::hot::ctx/set({
    "github.token": ::env/get("GITHUB_TOKEN", ""),
    "github.webhook.secret": ::env/get("GITHUB_WEBHOOK_SECRET", ""),
})
